#pragma once

enum ConfirmSource {KEYBOARD, JOYSTICK, MOUSE};
enum DpadDirection{LEFT, RIGHT, UP, DOWN};
enum GroupOrientation {VERTICAL, HORIZONTAL};
enum WideScreenMode {NOTSET=-1, NOWIDE = 0, SIXTEENBYNINE = 1, SIXTEENBYTEN = 2};
enum RootNodeType {BACKGROUND, SCENARIO, HUD};